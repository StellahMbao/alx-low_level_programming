#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strcmp - compares 2 strings
 * @s1: first string
 * @s2: second string
 */
int _strcmp(char *s1, char *s2)
{
	int compare = strcmp(s1, s2);

	return (compare);
}
